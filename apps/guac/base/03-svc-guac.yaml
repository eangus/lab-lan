---
apiVersion: v1
kind: Service
metadata:
  name: guacd
  namespace: guacd
  labels:
    app: guacd
spec:
  ports:
    - name: guac-port
      port: 8080
      # targetPort: 8080
  selector:
    app: guacd
  # type: LoadBalancer
